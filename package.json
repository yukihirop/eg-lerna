{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "versionup:patch": "lerna version patch --conventional-commits --no-push"
  },
  "devDependencies": {
    "lerna": "^5.3.0"
  }
}
